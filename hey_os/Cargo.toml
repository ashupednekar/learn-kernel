[package]
name = "hey_os"
version = "0.1.0"
edition = "2024"

[[bin]]
name = "hey_os"
test = false
bench = false

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"

[dependencies]
bootloader = "0.9" # latest includs core by default... 
